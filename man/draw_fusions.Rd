% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arriba.R
\name{draw_fusions}
\alias{draw_fusions}
\title{Draw fusion transcripts}
\usage{
draw_fusions(
  fusionsFile = "fusions.tsv",
  exonsFile = "annotation.gtf",
  outputFile = "output.pdf",
  alignmentsFile = "Aligned.sortedByCoord.out.bam",
  cytobandsFile = "cytobands.tsv",
  minConfidenceForCircosPlot = "medium",
  proteinDomainsFile = "protein_domains.gff3",
  sampleName = "",
  squishIntrons = TRUE,
  printExonLabels = TRUE,
  render3dEffect = TRUE,
  pdfWidth = 11.692,
  pdfHeight = 8.267,
  color1 = "#e5a5a5",
  color2 = "#a7c4e5",
  mergeDomainsOverlappingBy = 0.9,
  optimizeDomainColors = FALSE,
  fontSize = 1,
  fontFamily = "Helvetica",
  showIntergenicVicinity = "0",
  transcriptSelection = "provided",
  fixedScale = 0,
  coverageRange = "0"
)
}
\arguments{
\item{fusionsFile}{A path to the fusion TSV (Arriba or STAR-Fusion output).
Default: \code{"fusions.tsv"}.}

\item{exonsFile}{A path to the GTF annotation file.
Default: \code{"annotation.gtf"}.}

\item{outputFile}{The name of the PDF file to produce.
Default: \code{"output.pdf"}.}

\item{alignmentsFile}{A path to the sorted BAM file. If empty, coverage is not plotted.
Default: \code{"Aligned.sortedByCoord.out.bam"}.}

\item{cytobandsFile}{A path to a \code{cytobands.tsv} file for drawing ideograms and circos plots.
Default: \code{"cytobands.tsv"}.}

\item{minConfidenceForCircosPlot}{Minimum confidence level for fusions to be shown in circos plot. Must be one of \code{"none"}, \code{"low"}, \code{"medium"}, or \code{"high"}.
Default: \code{"medium"}.}

\item{proteinDomainsFile}{A path to a GFF3 of protein domain annotations.
Default: \code{"protein_domains.gff3"}.}

\item{sampleName}{A sample name to display as the page title.
Default: \code{""} (no title).}

\item{squishIntrons}{Whether introns should be visually "squished" to a constant length. Logical.
Default: \code{TRUE}.}

\item{printExonLabels}{Whether to parse and display the \code{exon_number} field in GTF. Logical.
Default: \code{TRUE}.}

\item{render3dEffect}{Whether to draw the 3D shading effect on exons/ideograms. Logical.
Default: \code{TRUE}.}

\item{pdfWidth}{Numeric width of the produced PDF.
Default: \code{11.692}.}

\item{pdfHeight}{Numeric height of the produced PDF.
Default: \code{8.267}.}

\item{color1}{HEX or R color string used for gene1.
Default: \code{"#e5a5a5"}.}

\item{color2}{HEX or R color string used for gene2.
Default: \code{"#a7c4e5"}.}

\item{mergeDomainsOverlappingBy}{Numeric fraction in (0,1) for how much overlap is required to merge two protein domains.
Default: \code{0.9}.}

\item{optimizeDomainColors}{Whether to re-assign domain colors to maximize visual contrast. Logical.
Default: \code{FALSE}.}

\item{fontSize}{Overall font size in the plots.
Default: \code{1}.}

\item{fontFamily}{Which font family to use in the PDF.
Must be available in \code{pdfFonts()}. Default: \code{"Helvetica"}.}

\item{showIntergenicVicinity}{Either numeric (how many bases around an intergenic breakpoint) or special keywords (\code{"closestGene"}, \code{"closestProteinCodingGene"}) that cause dynamic determination of range.
Default: \code{"0"} (no extra region).}

\item{transcriptSelection}{One of \code{"coverage"}, \code{"provided"}, or \code{"canonical"} controlling how to choose transcripts for the breakpoints.
Default: \code{"provided"}.}

\item{fixedScale}{If positive, forcibly scale transcripts to fit that many bases in the horizontal space. If 0, scales automatically.
Default: \code{0}.}

\item{coverageRange}{A character string with up to two numeric values (e.g., \code{"0"} or \code{"10,20"}) controlling coverage normalization.
"0" means automatic. If two values are given, each is used for gene1 and gene2 separately.
Default: \code{"0"}.}
}
\value{
Produces a PDF with the plots. Invisibly returns \code{NULL}.
}
\description{
This function reproduces the logic of the original script that draws fusion transcripts,
including validation of inputs, optional coverage, protein domains, circos plots, etc.
}
\examples{
\dontrun{
  draw_fusions(
    fusionsFile = "fusions.tsv",
    exonsFile = "annotation.gtf",
    outputFile = "output.pdf"
  )
}

}
